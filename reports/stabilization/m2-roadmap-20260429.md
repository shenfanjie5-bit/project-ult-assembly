# M2 Roadmap — Production Daily-Cycle Proof (Post-M1 Closure)

**Date:** 2026-04-29
**Status:** Roadmap / planning artifact. NO M2 sub-round executed yet.
**Author:** Project ULT v5.0.1 closure planning
**Predecessor:** M1 closed at 9/9 + 0 xfail + 0 deferred (commit `bca54d1` data-platform main, `6bb16bf` assembly main)

---

## Why M2

P5 (production shadow-run) is BLOCKED on **M2.6 production daily-cycle proof**.
M1 delivered the provider-neutral canonical_v2 + canonical_lineage substrate
that M2 reads from. M2 stitches together the runtime: real Dagster job + real
PG/Iceberg/Neo4j/DuckDB plus reasoner-runtime LLM calls + audit-eval retrospective
hook + 15 asset materializations.

> **Hard constraint from `ult_milestone.md`:** M2.6 is one full-cycle proof.
> It is **NOT** P5 shadow-run, and **NOT** M3/M4 work.

---

## M2 Closure Definition (verbatim from `ult_milestone.md`)

> **Goal**: execute one full production daily cycle with real Phase 0/1/2/3/audit
> runtimes and 15 assets.
>
> **Exit criteria**:
> - `production_daily_cycle_status()` no longer has unresolved runtime blockers for
>   the proven path.
> - The full proof includes Dagster run id, cycle id, 15 asset materializations,
>   formal snapshot ids, `cycle_publish_manifest`, audit/replay readback, and
>   retrospective hook result.
> - The proven path has passed M1.3/M1.4; it cannot rely on provider/raw-lineage
>   physical canonical schemas.
> - LLM evidence includes provider/model lineage, PII scrub boundary, and persisted
>   replay fields for formal calls.
> - **This still is not P5 shadow-run. It is one full-cycle proof.**

---

## The 6 RUNTIME_BLOCKERS (verbatim from `orchestrator/src/orchestrator_adapters/production_daily_cycle.py:46-53`)

```python
RUNTIME_BLOCKERS: Final[tuple[str, ...]] = (
    "configured_data_platform_current_cycle_runtime",
    "configured_graph_phase0_status_runtime",
    "configured_graph_phase1_runtime",
    "configured_reasoner_runtime",
    "configured_audit_eval_retrospective_hook_runtime",
    "production_current_cycle_dagster_run_evidence",
)
```

Each blocker is a named "configured runtime" that `production_daily_cycle_status()`
returns as `RUNTIME_BLOCKERS`. The function shape (line 402) means **as long as any
one of these 6 is fail-closed-stubbed, M2 is not closed.**

> **2026-04-29 fact correction:** the original draft of this roadmap listed 5
> blockers; it omitted `configured_data_platform_current_cycle_runtime`. The
> M2.0 readiness audit (`m2-0-runtime-readiness-audit-20260429.md`) inspected
> the source-of-truth tuple and corrected the count to 6. All downstream
> sub-round counts in this document reflect the corrected total.

Plus 1 environmental blocker (not in tuple but documented in the gap-audit):
- **Phase 2 pool failure rate metric**: `ORCHESTRATOR_PHASE2_POOL_FAILURE_RATE_METRIC_ARTIFACT`
  must point at real L8 metric output (or substitute artifact).

---

## 15-Asset Inventory for the Full-Cycle Proof

| Phase | Count | Assets |
|---|---|---|
| Phase 0 | 2 | `candidate_freeze`, `graph_status` |
| Phase 1 | 2 | `graph_promotion`, `graph_snapshot` |
| Phase 2 | 8 | `l1`, `l2`, `l3`, `l4`, `l5`, `l6`, `l7`, `l8` |
| Phase 3 | 2 | `formal_objects_commit`, `cycle_publish_manifest` |
| Audit-Eval | 1 | `retrospective_hook` |

All 15 must materialize successfully in the M2.6 final-cycle proof.

---

## M2 Sub-Round Roadmap

### Cross-Module Ownership Map

| Sub-Round | Primary Module(s) | Status of Module Repo (commits since M1 baseline) |
|---|---|---|
| M2.0 | assembly | (this roadmap; no impl yet) |
| M2.1 | assembly + orchestrator | active (orchestrator HEAD: `6a4c42c` "Clarify production daily-cycle freeze non-claim") |
| M2.2 | data-platform | M1 baseline holds; minimal M2 work expected |
| M2.3 | graph-engine | active (HEAD: `fc4e083` "test: prove graph live closure with GDS"; phase1 provider assets present) |
| M2.4 | reasoner-runtime | active (HEAD: `025db5b` "reasoner: support codex structured health and strict schemas") |
| M2.5 | audit-eval | active (HEAD: `a038ce1` "fix: harden retrospective hook provenance checks") |
| M2.6 | orchestrator + assembly + all of above | NOT STARTED |

### Round Detail

#### M2.0 — Module readiness audit (DONE 2026-04-29)

**Owner:** assembly (planning only)

**Status:** **DONE** — see [`m2-0-runtime-readiness-audit-20260429.md`](m2-0-runtime-readiness-audit-20260429.md).

**Scope (as executed):**
- Audited each of the 4 cross-module repos (data-platform, graph-engine, reasoner-runtime, audit-eval) for which RUNTIME_BLOCKERS are READY / PARTIAL / STUBBED.
- Captured a single audit doc with per-blocker status, source file:line evidence, env-var inventory, and gap-to-M2.1 list.
- Compose-stack startup was deferred to M2.1 (this round was strictly read-only).

**Aggregate result:** 2 READY (#3, #5) + 2 PARTIAL (#1, #4) + 1 STUBBED (#2) + 1 N/A (#6).
**Critical-path finding:** Blocker #2 `configured_graph_phase0_status_runtime` is STUBBED
(no `Neo4jGraphStatusProvider` exists in graph-engine). This needs a dedicated
sub-round (proposed M2.3a) before M2.6 can run end-to-end.

**Test gate:** none (audit-only).
**Dependencies:** M1 closure merged (✓).
**Effort:** 1 round (~60 min, completed).

#### M2.1 — Runtime preflight (env vars, dbt CLI, compose service health)

**Owner:** assembly

**Scope:**
- Verify dbt CLI runnable inside dagster-daemon image OR confirm host runtime path.
- Verify compose PG + Neo4j + Iceberg catalog reachability from CLI.
- Set / document all required env vars: `AUDIT_EVAL_DUCKDB_PATH_ENV`, `ORCHESTRATOR_PHASE2_POOL_FAILURE_RATE_METRIC_ARTIFACT`, `ORCHESTRATOR_POLICY_PATH`, `ORCHESTRATOR_DEFINITIONS_PROFILE`, `ORCHESTRATOR_MODULE_FACTORIES`, reasoner provider profile.
- Re-validate the M1.10 controlled v2 proof on the new compose snapshot.

**Test gate:** `daily_refresh --mock` against compose PG passes (mirrors M1.10 result).
**Dependencies:** M2.0.

#### M2.2 — Data-platform current-cycle runtime wiring

**Owner:** data-platform

**Scope:**
- Confirm `CurrentCycleReadinessProvider()` reads live PG `cycle_metadata` + `cycle_candidate_selection`.
- Confirm `freeze_current_cycle_candidates()` runs against live PG (atomic FOR UPDATE OF candidate_queue SKIP LOCKED).
- Confirm `current_cycle_inputs` reads canonical_v2.* (NOT legacy canonical.*); under `DP_CANONICAL_USE_V2=1`.

**Test gate:** integration test exercising live PG cycle freeze + readback.
**Dependencies:** M2.1.

#### M2.3 — Graph runtime wiring (Phase 0 + Phase 1)

**Owner:** graph-engine

**Scope:**
- Replace `_FailClosedGraphStatusProvider()` with real Neo4j-backed provider.
- Wire `configured_graph_phase0_status_runtime` against live Neo4j.
- Wire `configured_graph_phase1_runtime` (promotion + snapshot).
- Recent graph-engine commit (`fc4e083` "prove graph live closure with GDS") suggests this is partially built. Audit + close gaps.

**Test gate:** Phase 0 status check + Phase 1 promotion + snapshot produce real artifacts; integration test in graph-engine repo.
**Dependencies:** M2.1.

#### M2.4 — Reasoner runtime hard-stop + LLM lineage

**Owner:** reasoner-runtime

**Scope:**
- Configure `P2_REASONER_PROVIDER` / `P2_REASONER_MODEL` (Codex OAuth, Claude Code OAuth, MiniMax — at least one).
- Hard-stop on auth failure (no fake fallback).
- LiteLLM + Instructor pinned with SHA-256.
- LiteLLM OTEL + PII scrub before any L4/L6/L7 call.
- Recent commit (`025db5b` "support codex structured health and strict schemas") suggests progress on this front.

**Test gate:** reasoner runtime health-check returns "healthy" against real provider; provider/model lineage emitted.
**Dependencies:** M2.1.

#### M2.5 — Audit-eval retrospective hook persistence

**Owner:** audit-eval

**Scope:**
- `AUDIT_EVAL_DUCKDB_PATH_ENV` points to real DuckDB file.
- `_EnvBackedRetrospectiveHookRuntime.run()` persists audit/replay rows durably.
- Replay fields (`sanitized_input`, `input_hash`, `raw_output`, `parsed_result`, `output_hash`) writable.
- Recent commit (`a038ce1` "harden retrospective hook provenance checks") suggests current focus.

**Test gate:** retrospective hook integration test with audit/replay readback.
**Dependencies:** M2.1 + M2.4 (M2.4 produces the rows that M2.5 persists).

#### M2.6 — Full Dagster job proof (P5-unlocking gate)

**Owner:** orchestrator + assembly (integration owner)

**Scope:**
```python
daily_cycle_job.execute_in_process(
    tags={"cycle_id": "CYCLE_20260429"},
    instance=DagsterInstance.from_config(...),
)
```

Must materialize **all 15 assets**:
- 2 Phase 0 (candidate_freeze, graph_status)
- 2 Phase 1 (graph_promotion, graph_snapshot)
- 8 Phase 2 (l1–l8)
- 2 Phase 3 (formal_objects_commit, cycle_publish_manifest)
- 1 Audit-eval (retrospective_hook)

**Test gate:**
- Dagster run id captured + persisted
- `cycle_publish_manifest` row exists with non-null formal snapshot ids
- audit/replay rows readable from DuckDB
- retrospective hook result emitted
- `production_daily_cycle_status().runtime_blockers == ()` (empty)

**Dependencies:** M2.1 + M2.2 + M2.3 + M2.4 + M2.5 (all 5 must be DONE).

**Output evidence:** `production-daily-cycle-full-proof-20260429.md` + JSON artifacts (Dagster run record, manifest, audit rows).

---

## Hard Rules for the M2 Track (carry over from M1)

- `project_ult_v5_0_1.md` and `ult_milestone.md` UNCHANGED.
- No P5 shadow-run.
- No M3 / M4 entry.
- No frontend write API / API-6 / sidecar / Kafka / Flink / Temporal / news / Polymarket.
- Tushare remains `provider="tushare"` source adapter only.
- Treat `--mock` mode and local `/Volumes/dockcase2tb` archive as fixture data sources only — no live Tushare HTTP fetch unless explicitly authorized for one round.
- `/Users/fanjie/Desktop/BIG/FrontEnd/**` not modified.
- Each sub-round produces evidence file under `assembly/reports/stabilization/m2-X-...-20260429.md`.
- Sub-round commits use `m2-baseline-2026-04-29` branch (or per-round feature branches off it).
- M2.6 closure is a one-shot proof — not a repeating production cycle.

---

## Module Readiness Snapshot (informational, not yet audited in detail)

Recent commit titles hint that all 4 cross-module repos are actively closing
M2 prerequisites:

- **orchestrator** `6a4c42c` "Clarify production daily-cycle freeze non-claim" — clarifies what M2.6 cannot claim (preflight scope).
- **graph-engine** `fc4e083` "test: prove graph live closure with GDS" + `78019a7` "harden graph phase1 artifact proof" + `14c32f2` "add graph phase1 provider assets" — Phase 0/1 wiring underway.
- **reasoner-runtime** `025db5b` "support codex structured health and strict schemas" + `f5183f4` "Fix replay contract projection and dependency hash" + `66f4dc7` "Add gated LLM backend clients" — auth + replay closure underway.
- **audit-eval** `a038ce1` "harden retrospective hook provenance checks" + `13cff87` "require durable retrospective manifest lookup" — durability checks underway.

The M2.0 audit (`m2-0-runtime-readiness-audit-20260429.md`) executed on
2026-04-29 and recorded which of the 6 RUNTIME_BLOCKERS are READY / PARTIAL /
STUBBED at the orchestrator integration layer.

---

## Recommended First Round

**Status: M2.0 DONE 2026-04-29.** See `m2-0-runtime-readiness-audit-20260429.md`.

**Next: M2.1 — runtime preflight (compose stack + env vars).**

Per the M2.0 audit results (2 READY + 2 PARTIAL + 1 STUBBED + 1 N/A), the
critical-path finding is **blocker #2 `configured_graph_phase0_status_runtime`
is STUBBED** — graph-engine has no `Neo4jGraphStatusProvider` implementation.
A new sub-round **M2.3a** is proposed to land that before M2.6 can run.

Updated sequencing:

1. **M2.1** — compose stack + env vars (no source changes)
2. **M2.3a** (NEW) — implement Neo4jGraphStatusProvider in graph-engine
3. **M2.3b** — orchestrator Phase 0 → Phase 1 chain integration test
4. **M2.2** — data-platform live PG integration test (parallel with M2.3a)
5. **M2.4** — reasoner-runtime credential provisioning (mostly ops)
6. **M2.5** — audit-eval end-to-end smoke under compose
7. **M2.6** — full Dagster job proof (P5-unlocking gate)

M2.2 + M2.3a can run in parallel worktrees off `m2-baseline-2026-04-29` (similar
to M1.12 + M1.13 pattern). M2.4 + M2.5 are mostly ops/credential work and can
parallelize with M2.3a.

---

## Cross-References

- M1 closure progress: [`m1-legacy-retirement-preconditions-progress-20260428.md`](m1-legacy-retirement-preconditions-progress-20260428.md)
- M1.14 cleanup proof: [`m1-14-cleanup-proof-20260429.md`](m1-14-cleanup-proof-20260429.md)
- M1.10 controlled v2 proof results: [`m1-10-controlled-v2-proof-results-20260429.md`](m1-10-controlled-v2-proof-results-20260429.md) — this is what M2.6 must extend to a full cycle
- Production daily-cycle gap audit (Apr 28): [`production-daily-cycle-gap-audit-20260428.md`](production-daily-cycle-gap-audit-20260428.md)
- P4 controlled live bridge: [`p4-controlled-live-bridge-20260428.md`](p4-controlled-live-bridge-20260428.md)
- P1/P2 production daily-cycle proof: [`p1-p2-production-daily-cycle-proof-20260428.md`](p1-p2-production-daily-cycle-proof-20260428.md)
