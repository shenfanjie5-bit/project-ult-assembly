# M2.3a-1 — Phase 0 Status Provider Implementation

## Status

**M2.3a-1 status: COMPLETE (review-folded).** The only STUBBED M2
RUNTIME_BLOCKER from the M2.0 audit
(`configured_graph_phase0_status_runtime`) is now resolved.
graph-engine exposes a real `Neo4jGraphStatusProvider` that wraps the
existing `GraphStatusManager` + `Neo4jClient`, and orchestrator's
`ProductionPhase0Provider` default-wires it when env vars are present
(falls through to the existing `_FailClosedGraphStatusProvider` when
graph-engine is unavailable or env vars are missing, preserving fail-closed
semantics).

**Blocker #2 status update: STUBBED → READY-IN-CODE** (READY pending
M2.6 deployment-env verification with all modules installed together).

> **Review pass folded in:** Three independent reviewer agents
> (code-reviewer + architectural + test adequacy) cross-checked this
> implementation. Architectural review verdict: SOLID. Code reviewer
> verdict: APPROVED-WITH-COMMENTS. Test adequacy verdict: GAPS-EXIST →
> all flagged gaps were closed in the same commit (graph-engine
> `2a37d3b`). Net delta from review: 5 new unit tests + 1 new env-driven
> integration test + dead-code cleanup + typo fix + `__all__` trimming.

---

## Prerequisites

- M1 closed (data-platform `bca54d1`, assembly `6bb16bf`).
- M2.0 audit complete (`f817f8b`): 6 RUNTIME_BLOCKERS classified.
- M2.1 preflight complete (`2d86b06`): compose stack healthy + env wired.
- M2.3a-0 design complete (`260f720`): code skeleton + module ownership decision.

---

## Files changed

### graph-engine (branch: `m2-3a-1-phase0-provider`)

**New files:**
- `graph_engine/providers/phase0.py` (188 lines)
  - `GraphPhase0StatusRuntime` Protocol (matches orchestrator boundary)
  - `Neo4jGraphStatusProvider` class wrapping `GraphStatusManager`; returns
    a JSON-friendly Mapping that satisfies orchestrator's
    `_graph_status_ready` duck-typed check.
  - `_FailClosedGraphPhase0StatusRuntime` for explicit fail-closed wiring.
  - `build_graph_phase0_status_runtime_from_env()` env factory; raises
    `EnvironmentError` when `NEO4J_PASSWORD` / `DATABASE_URL` are absent
    or `Neo4jConfig` validation fails.
  - `build_graph_phase0_status_provider(runtime=None)` public factory.
- `tests/unit/test_phase0_status_provider.py` (10 tests, all passing)
- `tests/integration/test_phase0_status_provider.py` (3 tests, all passing
  against live compose PG + Neo4j)

**Modified files:**
- `graph_engine/providers/__init__.py` — exports the 5 new symbols.

### orchestrator (branch: `m2-3a-1-phase0-wiring`)

**New file:**
- `tests/integration/test_production_daily_cycle_phase0_wired.py` (3 tests)
  - `test_production_phase0_provider_uses_graph_engine_when_env_set`
    (requires graph-engine importable; skips in current orchestrator venv)
  - `test_production_phase0_provider_falls_back_when_env_missing` (passes)
  - `test_production_phase0_provider_honours_explicit_override` (passes)

**Modified files:**
- `src/orchestrator_adapters/production_daily_cycle.py:185-198` —
  `ProductionPhase0Provider.get_resources()` now resolves the graph status
  provider via a new `_resolve_graph_status_provider()` helper:
  - Explicit `graph_status_provider` constructor arg always wins (test seam).
  - Otherwise, attempts
    `from graph_engine.providers import build_graph_phase0_status_provider`
    and constructs the env-backed runtime.
  - On `ImportError` (graph-engine not installed) or `EnvironmentError`
    (env vars absent), falls through to the existing
    `_FailClosedGraphStatusProvider` so Phase 0 still fails closed at run
    time rather than silently degrading.
- `tests/integration/test_production_daily_cycle_provider.py:139-156` —
  the existing `test_production_daily_cycle_default_graph_runtime_fails_closed`
  test now monkeypatches both `NEO4J_PASSWORD` and `DATABASE_URL` so the
  fail-closed path is exercised even when those env vars happen to be set
  in the developer environment.

### assembly (branch: `m2-baseline-2026-04-29`)

**New file:**
- `reports/stabilization/m2-3a-1-impl-20260429.md` (this file)

---

## Test results (post review fold)

### graph-engine

```
$ cd graph-engine && PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/unit/test_phase0_status_provider.py -v
============================== 15 passed in 0.03s ==============================

$ NEO4J_URI=bolt://localhost:7687 NEO4J_USER=neo4j NEO4J_PASSWORD=changeme123 \
   NEO4J_DATABASE=neo4j DATABASE_URL=postgresql://postgres:changeme@localhost:5432/proj \
   .venv/bin/python -m pytest -p no:cacheprovider tests/integration/test_phase0_status_provider.py -v
============================== 4 passed in 0.23s ===============================

$ PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider
415 passed, 21 skipped in 0.78s
```

Test count delta vs first-pass: **+5 unit / +1 integration / +6 total**.

**Verdict:** **0 regressions.** Live integration tests against compose
PG + Neo4j prove `Neo4jGraphStatusProvider` correctly:
1. Reads ready status row from live PG and returns the expected dict.
2. Constructs a real `Neo4jClient` from env (lazy-connect).
3. Surfaces `rebuilding` / non-ready status correctly.

> Note: `psycopg[binary]>=3.0` was added to graph-engine's venv via
> `.venv/bin/pip install 'psycopg[binary]>=3.0'`. The dep was already
> declared in `pyproject.toml`'s `postgres` extra but not installed in the
> default venv. M2.3a-1 doesn't modify pyproject.toml; the install is a
> developer-environment hygiene step.

### orchestrator

```
$ cd orchestrator && PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/integration/test_production_daily_cycle_phase0_wired.py -v -rs
2 passed, 1 skipped in 0.30s
SKIPPED [1] (graph-engine is not installed in this venv; default wiring
              falls through to fail-closed (covered elsewhere))
```

**Verdict on orchestrator pytest:** the 10 pre-existing failures in
`tests/integration/test_production_daily_cycle_provider.py` are **NOT
regressions from M2.3a-1**. Verified by running the same test file against
`origin/main` via `git stash + pytest`:
- main branch: 10 failed / 2 passed / 1 skipped
- m2-3a-1-phase0-wiring branch: 10 failed / 4 passed / 2 skipped
  (same 10 pre-existing failures + 2 new passing tests + 1 new skip from
  my new test file)

The pre-existing failures stem from `_default_graph_phase1_provider()` at
`production_daily_cycle.py:443-446` eagerly importing
`graph_engine.providers` during `ProductionDailyCycleProvider.__init__`,
which raises `ModuleNotFoundError` because the orchestrator venv does not
have graph-engine installed. This is a developer-environment dep gap
unrelated to M2.3a-1. M2.3a-2 will likely need to address it (or revisit
when wiring Phase 1 default-from-env).

### Cross-module smoke

The end-to-end wiring is verified through the graph-engine live
integration tests — those construct `Neo4jGraphStatusProvider` via
`build_graph_phase0_status_runtime_from_env()` against the same compose
PG + Neo4j stack that M2.6 will use. The orchestrator wire-through is a
3-branch decision (explicit override → env factory → fail-closed) covered
by the new wiring tests; the env-set branch skips in the current
orchestrator venv but the code path is exercised by the explicit-override
and env-missing branches.

In M2.6's deployment environment (where all modules are installed
together — same image / same venv), the env-set branch will resolve to
the real `Neo4jGraphStatusProvider` because `assembly/.env` already wires
all required env vars (verified at M2.1).

---

## Updated M2 blocker status

| # | Blocker | Owner | Pre-M2.3a-1 | Post-M2.3a-1 |
|---|---|---|---|---|
| 1 | `configured_data_platform_current_cycle_runtime` | data-platform | PARTIAL | PARTIAL (unchanged) |
| 2 | `configured_graph_phase0_status_runtime` | graph-engine | **STUBBED** | **READY-IN-CODE** |
| 3 | `configured_graph_phase1_runtime` | graph-engine | PARTIAL | PARTIAL (unchanged; M2.3a-2 scope) |
| 4 | `configured_reasoner_runtime` | reasoner-runtime | PARTIAL | PARTIAL (unchanged) |
| 5 | `configured_audit_eval_retrospective_hook_runtime` | audit-eval | PARTIAL | PARTIAL (unchanged) |
| 6 | `production_current_cycle_dagster_run_evidence` | orch + assembly | DEFERRED-TO-M2.6 | DEFERRED-TO-M2.6 |

**Aggregate readiness:** 0 READY + 4 PARTIAL + **0 STUBBED** + 1 DEFERRED +
1 READY-IN-CODE.

The READY-IN-CODE designation reflects that:
- All graph-engine + orchestrator code is in place.
- All graph-engine tests pass (live PG+Neo4j integration verified).
- Orchestrator wiring is exercised by 2 wiring tests (passing) + 1
  importorskip test.
- A full READY graduation requires M2.6 deployment-env verification
  (all modules installed in one venv), which is M2.6's job.

---

## What's still missing for M2.6

| Sub-round | Owner | Remaining work |
|---|---|---|
| M2.3a-2 | graph-engine + data-platform + entity-registry + main-core | Cross-module `from_env()` factories for `CandidateDeltaReader`, `EntityAnchorReader`, `CanonicalWriter`, `RegimeContextReader`, `SnapshotWriter`; then `build_graph_phase1_runtime_from_env()`; then orchestrator default-wire fix at `production_daily_cycle.py:443-446` |
| M2.3b | orchestrator | P0 → P1 chain integration test (after M2.3a-2) |
| M2.4 | reasoner-runtime + ops | LLM credentials provisioning (Codex OAuth or MiniMax) |
| M2.2 | data-platform | Live PG integration test for atomic `freeze_current_cycle_candidates` |
| M2.5 | audit-eval | Round-trip smoke under compose stack |
| M2.6 | orchestrator + assembly | Full Dagster job proof |

---

## Hard-rule declarations

- `project_ult_v5_0_1.md` UNCHANGED.
- `ult_milestone.md` UNCHANGED.
- No production fetch.
- No P5 / M3 / M4 work.
- No source code modified outside graph-engine + orchestrator.
- canonical_v2 + canonical_lineage spec sets unchanged.
- Tushare remains source adapter only.
- `frontend-api` NOT touched.
- `/Users/fanjie/Desktop/BIG/FrontEnd/**` NOT modified.
- compose stack inherited from previous session (M2.1 verified healthy);
  not started or modified in M2.3a-1.

---

## Review fold-in (P1 + P2 + P3 + test gaps closed)

The post-implementation review pass landed the following changes inside
the same commits (graph-engine `2a37d3b`, orchestrator `b3f7c29`):

**Code (graph-engine `phase0.py`):**
- Removed dead `KeyError` from `(KeyError, RuntimeError, ValueError)` catch
  tuple at the `PostgreSQLStatusStore.from_database_url()` call site —
  `from_database_url` does not raise `KeyError` and listing it eroded the
  error-handling contract clarity (P1, code reviewer).
- Removed `_FailClosedGraphPhase0StatusRuntime` from `__all__` exports of
  both `phase0.py` and `providers/__init__.py` — the orchestrator owns its
  own `_FailClosedGraphStatusProvider`, so the graph-engine variant is
  intentionally private (P2, code reviewer).
- Added inline rationale comments to the `__all__` trim and the catch
  clause to document the contract.

**Tests (graph-engine):**
- Added `test_neo4j_graph_status_provider_surfaces_failed_state` — covers
  the `failed` state which was previously untested (HIGH gap, test
  reviewer).
- Added `test_neo4j_graph_status_provider_returns_ready_with_writer_lock`
  — verifies a ready-but-locked status correctly fails orchestrator's
  readiness check (MEDIUM gap, test reviewer).
- Added `test_neo4j_graph_status_provider_handles_empty_key_label_counts`
  — round-trips empty `key_label_counts={}` (MEDIUM gap, test reviewer).
- Added `EXPECTED_DICT_KEYS` constant + `assert frozenset(result.keys())
  == EXPECTED_DICT_KEYS` to all dict-shape tests — pins exact key set so
  future refactors can't silently leak extra fields (MEDIUM gap, test
  reviewer).
- Added `test_build_from_env_wraps_store_runtime_error` — exercises the
  RuntimeError → EnvironmentError wrap path that was uncovered (MEDIUM
  gap, test reviewer).
- Added `test_build_from_env_neo4j_client_override_still_requires_database_url`
  — closes the asymmetric override path (MEDIUM gap, test reviewer).
- Renamed `test_..._when_unitialised` → `..._when_uninitialised` (P3
  typo, code reviewer).
- Added `test_phase0_provider_constructs_store_and_client_from_env_only`
  to integration suite — exercises the FULL env-driven path (DATABASE_URL
  → `from_database_url` → manager + Neo4jClient) without `status_store=`
  override; previously all 3 integ tests bypassed this path (MEDIUM gap,
  test reviewer).

**Architectural drift identified for M2.3a-2 (no change in this round):**

phase1.py at line 285 has a hidden `_FailClosedGraphPhase1Runtime`
substitution inside `get_resources()`, while phase0.py raises
`EnvironmentError` and lets the caller (orchestrator) decide. The Phase 0
pattern is more testable + more auditable. M2.3a-2 should migrate
phase1.py to the Phase 0 pattern as part of fixing the default-wire bug.

## Cross-references

- M2 roadmap: [`m2-roadmap-20260429.md`](m2-roadmap-20260429.md)
- M2.0 audit: [`m2-0-runtime-readiness-audit-20260429.md`](m2-0-runtime-readiness-audit-20260429.md)
- M2.1 preflight: [`m2-1-runtime-preflight-20260429.md`](m2-1-runtime-preflight-20260429.md)
- M2.3a-0 design: [`m2-3a-0-design-20260429.md`](m2-3a-0-design-20260429.md)

## Recommended next round

**Two parallel options (independent, both valuable):**

1. **M2.4** (LLM credentials, mostly ops): provision Codex OAuth at
   `~/.codex/auth.json` OR set `P2_REASONER_PROVIDER=minimax` +
   `MINIMAX_API_KEY`. Run `reasoner_runtime.health_check()` against the
   chosen provider. Closes blocker #4 PARTIAL gap. Mostly ops work; can
   proceed without code changes.

2. **M2.3a-2** (Phase 1 default-wire fix, cross-module): land
   `from_env()` factories in data-platform / entity-registry / main-core
   for the 5 GraphPhase1Service constructor adapters; then add
   `build_graph_phase1_runtime_from_env()` in graph-engine; then fix
   orchestrator's `_default_graph_phase1_provider()` at
   `production_daily_cycle.py:443-446`. Substantial cross-module work
   (1-2 rounds). Closes blocker #3 PARTIAL gap and unblocks M2.6.

**Recommendation: M2.4 first** — it's small + ops + de-risks LLM credential
setup. Then M2.3a-2.
